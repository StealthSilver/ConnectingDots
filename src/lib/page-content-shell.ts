/**
 * Centered `max-w-7xl` + horizontal padding. Matches the outer frame of
 * `PageGridLines` (vertical guide edges).
 */
export const pageContentShellClassName =
  "mx-auto w-full max-w-7xl px-3 sm:px-4 lg:px-5";
