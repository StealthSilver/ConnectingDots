/** Centered `max-w-7xl` + horizontal padding (navbar / hero / sections). */
export const pageContentShellClassName =
  "mx-auto w-full max-w-7xl px-4 sm:px-5 lg:px-6";

/** Scrollable content area inside `AppPageShell` (home, blog, learning, etc.). */
export const appPageSectionClassName =
  "mx-auto w-full max-w-6xl px-4 pb-12 pt-4 sm:px-5 sm:pb-16 sm:pt-5 lg:px-6";
